# ФОРТОЧКА — контекст проекта для Claude

Это handoff-документ. Прочитай его целиком перед работой — он описывает что
за проект, что уже сделано, что за что отвечает и на чём продолжать.

---

## 1. Что это за проект

**ФОРТОЧКА** — позитивное новостное медиа («только хорошие новости») для
русскоязычной аудитории. Полностью автоматизированный конвейер:

```
RSS-источники → ИИ-фильтр (отсев) → ИИ-редактор (переписывание по-русски)
            → ручной апрув главреда в админке → публикация на сайт + Telegram
```

Бренд раньше назывался ТАКС, переименован в ФОРТОЧКА (слоган «глоток свежих
новостей»). Причина: ТАКС созвучно с ТАСС — риск для новостного бренда.

Расположение: `D:\Боты\ТАКС\ТАКС\app-taks` (Next.js-проект).
Папка `D:\Боты\ТАКС\ТАКС\` — корень, там лежат демо-файлы полянки (см. ниже).

---

## 2. Стек и архитектура

- **Сайт + админка**: Next.js 14 (App Router) + React + TypeScript. Деплой задуман на Vercel.
- **База**: Supabase (Postgres). СЕЙЧАС используется ЛОКАЛЬНЫЙ Supabase через
  Docker (`supabase start`), потому что у пользователя в РФ были проблемы с
  облаком. Облачный проект тоже создан (`vecmunfotagjmfhxreob.supabase.co`),
  но для тестов работаем локально.
- **Воркер**: Node/TypeScript, запускается через `tsx`. Шаги: collect → filter → draft.
- **ИИ**: Groq (Llama 3.3 70B) — фильтр; Google Gemini 2.5 Flash — редактор. Ключи бесплатные.
- **Telegram**: публикация в канал через Bot API (опционально, сейчас не настроен до конца).

### Структура файлов
- `app/page.tsx` — лента новостей (главная, все рубрики)
- `app/cat/[slug]/page.tsx` — лента рубрики, своя og-метадата, пререндерится статически
- `app/article/[slug]/page.tsx` — страница статьи + generateMetadata (og:image, og:type=article)
- `app/admin/page.tsx` + `components/AdminQueue.tsx` — админка: очередь на апрув
- `app/admin/sources/page.tsx` + `components/SourcesAdmin.tsx` — управление RSS-источниками
- `app/admin/logs/page.tsx` — экран pipeline_log с фильтрами по шагу/статусу
- `app/api/admin/decide/route.ts` — API: опубликовать/отклонить + revalidatePath
- `app/api/admin/sources/route.ts` — GET/POST/PATCH/DELETE для источников
- `app/sitemap.ts` + `app/robots.ts` — SEO
- `app/layout.tsx` — глобальная og/twitter метадата + metadataBase из SITE_URL
- `components/NewsFeed.tsx` — общий рендер ленты (lead + briefs + grid), используется главной и /cat
- `lib/db.ts` — Supabase: sbService() (обходит RLS, воркер/админ) и sbAnon() (публичные чтения)
- `lib/sanitize.ts` — isomorphic-dompurify, чистит body_html на шаге draft (whitelist тегов/URI)
- `lib/llm.ts` — клиенты Groq и Gemini, промпты фильтра и редактора
- `lib/telegram.ts` — публикация в TG (безопасно пропускается без ключей)
- `lib/types.ts` — типы, список категорий (захардкожен, см. п.6 ниже)
- `lib/cover.ts` + `components/Cover.tsx` — генеративные SVG-обложки (фолбэк, когда нет фото)
- `worker/run.ts` — точка входа воркера (грузит .env.local через --env-file)
- `worker/steps/{collect,filter,draft}.ts` — шаги пайплайна
- `worker/scripts/resanitize.ts` — одноразовая миграция body_html через текущий sanitizeHtml
- `next.config.mjs` — images.remotePatterns: https/http ** (произвольные RSS-хосты)
- `supabase/migrations/20260101000000_init.sql` — схема БД (применяется при supabase start)
- `supabase/schema.sql` — та же схема для облака (SQL Editor)
- `supabase/add_ru_sources.sql`, `add_more_sources.sql` — добавление источников

### Статусная машина статьи (поле articles.status)
```
found → filtered → drafted → pending_review → published
      ↘ rejected_by_ai        ↘ rejected_by_editor   ↘ error (после 3 ретраев)
```

---

## 3. Что уже сделано ✅

- Проект переименован ТАКС → ФОРТОЧКА везде (UI, промпты, доки).
- Локальный Supabase через Docker поднят и работает. Схема применена миграцией.
  В config.toml ОТКЛЮЧЕНЫ сервисы edge_runtime, studio, analytics — они качали
  недоступные из РФ образы и роняли `supabase start`. Не включай их обратно.
- В `.env.local` прописаны ЛОКАЛЬНЫЕ ключи Supabase (стандартные demo-ключи CLI,
  127.0.0.1:54321) + рабочие ключи Groq и Gemini.
- Воркер запускается через `--env-file=.env.local` (в package.json scripts).
  Добавлена зависимость dotenv.
- **Весь пайплайн прогнан вживую и работает**: collect собрал ~69 новостей,
  filter отобрал, draft переписал ~15 статей по-русски. Они в админке.
- **13 RSS-источников** (проверены на доступность):
  - РФ: Позитивные новости, Naked Science, N+1, Hi-News, ТАСС Наука
  - Мир: Good News Network, Positive News, ScienceDaily (+Health/Animals/Space), NASA
  - Phys.org отдаёт 451 — выключай в админке /admin/sources, не через SQL.
- Категории (id): science=1, animals=2, ecology=3, kindness=4, space=5, tech=6, lifehacks=7.

### Что доделано в стадии «структурные улучшения»
- **Картинки к статьям**: расширен `pickImage` в collect.ts (media:thumbnail, itunes:image,
  description/summary, enclosure по расширению, абсолютизация относительных URL).
  Если из RSS картинку вытащить не вышло — фолбэк через fetch og:image/twitter:image
  со страницы статьи (таймаут 8 сек). В Cover.tsx у `<img>` стоит
  `referrerPolicy="no-referrer"` против hotlink-блоков.
- **Санитизация**: `lib/sanitize.ts` (isomorphic-dompurify) чистит body_html в шаге draft.
  Whitelist тегов: p,br,strong,em,b,i,u,a,ul,ol,li,blockquote,h2-h4,figure,figcaption,img.
  URI: только http(s)/mailto/якоря/относительные. Старые статьи можно пересанитизировать
  через `npm run worker:resanitize`.
- **SEO**: `/sitemap.xml` (главная + 7 рубрик + последние 500 статей), `/robots.txt`
  (запрещены /admin и /api), per-article og/twitter метаданные, per-category тоже.
  При апруве `revalidatePath('/')`, `/article/[slug]`, `/sitemap.xml`.
- **Роуты рубрик**: `/cat/[slug]` вместо `?cat=`. `generateStaticParams` пререндерит
  все 7 рубрик на билде. Masthead ссылается на `/cat/...`.
- **Админка источников**: `/admin/sources?key=...` — добавление, вкл/выкл, удаление,
  рубрика по умолчанию, last_fetched_at. DELETE откажет, если у источника есть
  привязанные статьи (FK без on delete) — это by design, чтобы не терять историю.
- **Логи воркера**: `/admin/logs?key=...` — последние 300 записей pipeline_log с
  фильтрами по шагу и статусу OK/Ошибка. Названия статей подтягиваются join'ом.
- **Supabase-клиенты разделены**: `sbService()` (сервисный ключ, обходит RLS — воркер
  и админ-API) и `sbAnon()` (анонимный ключ, под RLS — публичные чтения сайта).
  Алиас `sb` смотрит на сервисный для совместимости.
- **`next.config.mjs`**: добавлены `remotePatterns` (https/http **). Перед деплоем
  на Vercel поменять `unoptimized: true` на `false`, чтобы включить WebP/AVIF.

### Релакс-окно («Пойти на полянку») — отдельная большая фича, СЕЙЧАС НА ПАУЗЕ
- `components/RelaxWindow.tsx` + `lib/relax/engine.ts` + `lib/relax/sound.ts` —
  анимированная лужайка на Canvas (трава, коты, птицы, бабочки, мышь-курсор) + звук.
- Демо: `../relax-demo.html`, макет сайта с кнопкой: `../site-mockup.html`.
- Звук: реальные сэмплы пользователя в `public/sounds/` (ветер, птицы, пчёлы,
  мяу, мурчание); ветер лупится бесшовно (loopXfade).
- Коты: СЕЙЧАС рисованные кодом (выглядят посредственно — пользователь недоволен).
  В движок заложена поддержка PNG-спрайтов: положить кадры в `public/sprites/cats/`
  (имена в README там же) — движок подхватит их вместо рисованных. Пользователь
  ищет качественные CC0-спрайты. До этого полянку НЕ доделываем.

---

## 4. На чём продолжить (приоритет сверху вниз)

### Качество статей (приоритет №1 — самые большие победы)
1. **Полнотекстовый парсинг исходной статьи** — главная боль на сейчас. Многие
   RSS-фиды (ScienceDaily, Good News Network, NASA News) отдают только аннотацию
   в 300-500 символов. Groq из этого пишет 3 коротких абзаца. Нужен модуль
   `lib/article-extract.ts`: идёт на `source_url`, выдёргивает основной текст,
   подставляет в `raw_text`. После этого LLM видит 3000-8000 символов вместо
   тизера. **СТАРТУЕМ ОТСЮДА.**
2. **Few-shot примеры в промпте `EDITOR_SYSTEM`** — добавить в `lib/llm.ts`
   1-2 образцовых пары «вход → эталонный выход». Llama 3.3 70B значительно лучше
   копирует тон и структуру, чем по голым правилам.
3. **Раздельные промпты под рубрики** — для «науки» терминологический,
   для «лайфхаков» разговорный. Словарь `{categoryId → доп. инструкция}`.

### SEO и распространение
4. **JSON-LD `NewsArticle`** на странице статьи — попадание в Google News
   и нормальные карточки в выдаче. ~30 строк в `app/article/[slug]/page.tsx`.
5. **Авто-генерация OG-картинок** через `@vercel/og` — красивые превью для
   шеров в Telegram/WhatsApp с заголовком, рубрикой и логотипом. Полдня.
6. **Собственный RSS-фид** `/rss.xml` — для подписки внешних агрегаторов.

### Визуал и UX
7. **Прогресс-бар чтения** над страницей статьи (тонкая линия цвета рубрики).
8. **Тёмная тема** — Spectral + IBM Plex отлично работают в обоих режимах.
9. **Lightbox на инлайн-фото** — клик по картинке внутри статьи открывает
   её в полный экран. Особенно для статей с галереями.

### Админка и редполитика
10. **Дедуп по содержанию** — TF-IDF близость с уже опубликованными за 7 дней.
    Сейчас дедуп только по `source_url`, один сюжет приходит с 5 источников.
11. **Diff между версиями черновика** при «↻ Groq/Gemini» — сохранять предыдущую
    версию в `articles.previous_draft`, кнопка «вернуть прошлую».
12. **Отложенная публикация** — поле `scheduled_at`, фоновая публикация в
    нужное время.

### Стабильность и инфра
13. **Telegram-публикация** — настроен и работает. Можно подкрутить формат поста.
14. **Деплой в облако** (Vercel + облачный Supabase) — когда наиграется локально.
    При деплое: SITE_URL в env, images.unoptimized → false, проверить, что
   anon-ключ есть в .env (sbAnon фолбэкает на сервисный, но в проде нужен честный anon).
4. **«Мемошная»** — вторая запланированная фича (вкладка с мемами + автосбор).
   Ещё не начата. ВМЕСТЕ с ней имеет смысл сделать G (см. ниже).
5. **Релакс-полянка** — продолжить, когда пользователь даст спрайты котов.

### Отложено сознательно
- **G. Категории из БД вместо хардкода в `lib/types.ts`.** Список `CATEGORIES`
  используется повсеместно через синхронный `catById()` (главная, NewsFeed,
  Masthead, AdminQueue, SourcesAdmin). Переводить на async-фетч сейчас — большой
  рефакторинг ради того, чтобы редактировать список, который в принципе не
  меняется. Делать ВМЕСТЕ с «Мемошной», когда реально понадобится UI рубрик.
- **H. Переключение на `next/image`.** Сейчас `images.unoptimized: true` и обычный
  `<img>` в Cover.tsx. `remotePatterns` уже добавлены. Перед деплоем убрать
  unoptimized и поменять `<img>` на `<Image fill>` (требует ручной проверки
  каждой картинки на разных хостах).

---

## 5. Как запустить локально

Нужен запущенный **Docker Desktop**. Терминал в папке `app-taks`.

```bash
supabase start              # поднять локальную БД (данные сохраняются между запусками)
npm run dev                 # сайт на http://localhost:3000 (занимает терминал!)
```

Воркер (во ВТОРОМ терминале, пока сайт крутится в первом):
```bash
npm run worker:collect      # RSS → found
npm run worker:filter       # Groq отбирает (found → filtered/rejected_by_ai)
npm run worker:draft        # Gemini переписывает (filtered → pending_review)
npm run worker:resanitize   # одноразово пройти sanitize по всем старым body_html
```
filter/draft берут новости пачками — для обработки всего запусти 2-3 раза.

Админка (ключ = ADMIN_KEY из .env.local):
- `http://localhost:3000/admin?key=fortochka2026secret` — очередь на апрув
- `http://localhost:3000/admin/sources?key=...` — RSS-источники
- `http://localhost:3000/admin/logs?key=...` — логи воркера

SQL к локальной базе: `supabase db query "..."` (ОДИН запрос за раз — несколько
команд CLI не принимает).

---

## 6. Важные нюансы и грабли

- **Обрывы записи файлов.** В ходе разработки большие файлы (engine.ts, sound.ts,
  globals.css, миграция) НЕОДНОКРАТНО обрезались/портились при записи (потеря
  хвоста, null-байты, битая кириллица). ВСЕГДА после правки большого файла
  проверяй: баланс скобок, что файл заканчивается корректно, нет null-байтов,
  компилируется ли (`npx tsc` или esbuild). При сборке демо-HTML проверяй
  `node --check` на вырезанном JS.
- **Кириллица в SQL.** Длинное тире «—» и спецсимволы в комментариях ломали
  миграцию. Комментарии в SQL держи на латинице или без экзотики.
- **Supabase: новые vs легаси ключи.** Новые ключи (`sb_publishable_`/`sb_secret_`)
  не работают со старой версией @supabase/supabase-js. Используй легаси-ключи
  формата `eyJ...` (для локалки — стандартные demo-ключи CLI).
- **Сеть в РФ.** Часть зарубежных RSS и Docker-реестров (ecr.aws, jsr.io)
  недоступны без VPN — отсюда отключённые сервисы Supabase и Phys.org 451.
- **Безопасность.** Облачные ключи (от supabase.co) и ключи Groq/Gemini
  засветились в переписке ранее — пользователю стоит их пересоздать перед
  публичным запуском. Локальные demo-ключи (127.0.0.1) безопасны.
- **Два терминала.** `npm run dev` занимает терминал — воркер и SQL запускай во втором.

---

## 7. Тон работы с пользователем

Пользователь — не программист, объясняй простым языком, по шагам, без жаргона.
Команды давай готовыми к копипасту. Проверяй факты в коде, а не по памяти.
Будь честен про ограничения (например, среда без интернета не запустит воркер —
это делает пользователь у себя). Не плоди лишних итераций.
